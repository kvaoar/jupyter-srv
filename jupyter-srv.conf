JUPYTER_IP=0.0.0.0
JUPYTER_PORT=8080

# Safer default: keep token enabled unless you explicitly disable it.
JUPYTER_TOKEN_ARG=

# For open access in LAN without token, uncomment:
# JUPYTER_TOKEN_ARG=--ServerApp.token=''

JUPYTER_EXTRA_ARGS=
